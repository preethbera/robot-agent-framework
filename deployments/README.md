# Deployment Specifications

Runtime instance configuration belongs at `<deployment-id>.yaml` and references
built Agent Definitions. It must not duplicate Agent semantics. Multi-agent
deployment implementation begins in M6.

